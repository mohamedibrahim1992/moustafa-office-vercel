{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/db run build; pnpm --filter @workspace/api-zod run build; pnpm --filter @workspace/api-client-react run build; pnpm --filter @workspace/accounting run build",
  "outputDirectory": "artifacts/accounting/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  }
}
